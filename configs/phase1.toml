# Working research choices for discussion with Dr. Augustine Twumasi.
[data]
ticker = "SPY"
start = "2014-11-01" # inclusive; warm-up history
end = "2026-01-01"   # exclusive
calendar = "NYSE"   # U.S. equity session dates, including exceptional closures

[research]
windows = [5, 10, 20]
horizon = 5
momentum_window = 5
baseline_window = 20

[split]
train_start = "2015-01-01"
validation_start = "2022-01-01"
test_start = "2024-01-01"
test_end = "2026-01-01" # exclusive; reserved, not evaluated in Phase 1

[paths]
# Relative to the repository root (the config file's parent directory's parent).
snapshot_dir = "data/raw/spy_2014-11-01_2026-01-01"
output_dir = "artifacts/runs"
